"""Diagrams drawn by this program rather than pasted into it.

A picture that lives in the repository as a `.png` drifts: the words
in it stop matching the words in the guide beside it, and nobody
notices because nobody diffs an image. Everything in this package is
generated from data, in the same token set as the interface, so a
diagram cannot fall out of step with the product it describes.
"""
